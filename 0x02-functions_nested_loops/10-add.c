#include <stdio.h>
#include "main.h"

/**
 * add - adds two integers together
 * @x: first integer
 * @y: second integer
 * Return: returns the sum of the two integers
 */

int add(int x, int y)
{
	return (x + y);
}
