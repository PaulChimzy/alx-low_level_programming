#include <stdio.h>
/**
 * main - prints some string or characters to the screen
 * Return: 0 if everything runs fine
 */
int main(void)
{
  printf("with proper grammar, but the outcome is a piece of art,\n");
  return (0);
}
