#include <stdio.h>

/**
 * main - prints lowercase alphabets
 * Return: returns 0 if everything works fine
 */

int main(void)
{
	int i;
	for (i = 97; i < 123; i++)
	{
		putchar(i);
	}
	putchar('\n');

	return (0);
}
